[package]
name = "ferrum-beacon"
version = "0.2.0"
edition = "2021"
description = "GA4GH Beacon v2 API"
license = "BUSL-1.1"

[dependencies]
ferrum-core = { path = "../ferrum-core", default-features = false }
ferrum-reference = { path = "../ferrum-reference" }
ferrum-federation = { path = "../ferrum-federation" }
axum = { version = "0.7", features = ["json"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
tower = "0.4"
utoipa = { version = "4", features = ["axum_extras"] }
utoipa-swagger-ui = { version = "6", features = ["axum"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"], optional = true }
sqlx = { version = "0.8", features = ["runtime-tokio", "postgres", "sqlite"] }
chrono = "0.4"
thiserror = "1"
anyhow = "1"

[dev-dependencies]
ferrum-core = { path = "../ferrum-core" }
http = "1"
tower = { version = "0.4", features = ["util"] }
hyper = "1"
axum = "0.7"
sqlx = { version = "0.8", features = ["runtime-tokio", "postgres", "sqlite", "migrate"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"

[[bin]]
name = "ferrum-beacon"
path = "src/main.rs"
required-features = ["standalone"]

[features]
default = []
# Enable with `ferrum-core` libdeflate (faster BGZF when you add noodles-based readers here).
libdeflate = ["ferrum-core/libdeflate"]
standalone = ["tracing-subscriber"]
